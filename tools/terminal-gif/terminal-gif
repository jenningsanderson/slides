#!/bin/sh
# Wrapper — delegates to the unified slides CLI.
# Prefer: uv run slides gif COMMAND output.gif [options]
# Note: requires 'terminal' extra: uv sync --extra terminal
exec uv run slides gif "$@"
