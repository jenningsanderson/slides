[project]
name = "slides-builder"
version = "0.1.0"
description = "Build Reveal.js presentations from Markdown slides — GitHub Action and local dev tool"
readme = "README.md"
requires-python = ">=3.11"
dependencies = [
    "markdown>=3.6",
]

[project.urls]
Repository = "https://github.com/jenningsanderson/slides"
